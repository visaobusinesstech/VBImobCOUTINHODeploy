import React from "react";

/**
 * Faixa de vencimento de licença removida por produto.
 * Mantida como no-op para não quebrar imports existentes.
 */
const SubscriptionAlertBanner = () => null;

export default SubscriptionAlertBanner;
